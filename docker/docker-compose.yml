version: "3.8"

services:
  jenkins:
    image: jenkins/jenkins:lts-jdk11
    container_name: jenkins-instance
    volumes:
      - jenkins-data:/var/jenkins_home:rw
    ports:
      - 127.0.0.1:8010:8080

volumes:
  jenkins-data:
    name: jenkins-data